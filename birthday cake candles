int birthdayCakeCandles(vector<int> candles) {
    int n=candles.size();
    int count=0,maxi=0;
for(int i=0;i<n;i++)
maxi=max(maxi,candles[i]) ;
 for(int i=0;i<n;i++)
 if(candles[i]==maxi)   
    count++;
return count;
}
